# AI Configuration
AI_PROVIDER=anthropic
AI_MODEL=claude-sonnet-4-20250514
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Messaging Channels
# Telegram bot tokens are now per-user (configured in profile)
WHATSAPP_ENABLED=false
WHATSAPP_API_TOKEN=

# Web
WEB_ENABLED=false
WEB_PORT=3000
JWT_SECRET=change-me
APP_URL=http://localhost:4200

# Email (magic link auth) — leave SMTP_HOST empty for dev mode (logs to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=BJJ Coach <coach@bjjcoach.app>

# Database
DATABASE_PATH=./data/bjj-coach.db

# Defaults
DEFAULT_TIMEZONE=America/New_York
